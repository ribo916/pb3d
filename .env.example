# Same Neon project as the sibling pickleball-drills repo — copy the pooled
# connection string from there (or the Neon dashboard), just used against a
# different table (pb3d_drills, see db/schema.sql) in the same database.
DATABASE_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/neondb?sslmode=require&channel_binding=require
